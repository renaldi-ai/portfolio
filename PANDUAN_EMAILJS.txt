# Panduan EmailJS untuk InfinityFree

## 🎯 Kenapa EmailJS?

InfinityFree memblokir port SMTP eksternal. EmailJS adalah solusi sempurna karena:
- ✅ Tidak pakai SMTP server
- ✅ Kirim email langsung dari browser (JavaScript)
- ✅ Bypass blocking InfinityFree
- ✅ GRATIS 200 email/bulan
- ✅ Setup 5 menit
- ✅ Tidak perlu backend PHP

---

## 🚀 Setup EmailJS (5 Menit)

### STEP 1: Daftar EmailJS

1. Buka: https://www.emailjs.com
2. Klik "Sign Up Free"
3. Isi form pendaftaran
4. Verify email
5. Login

### STEP 2: Add Email Service

1. Setelah login, klik "Email Services" (menu kiri)
2. Klik "Add New Service"
3. Pilih provider:
   - **Gmail** (recommended)
   - Outlook
   - Yahoo
   - Custom SMTP
4. Jika pilih Gmail:
   - Klik "Connect Account"
   - Login dengan Gmail Anda
   - Allow permissions
5. Klik "Create Service"
6. **COPY Service ID** (contoh: service_abc123)

### STEP 3: Create Email Template

1. Klik "Email Templates" (menu kiri)
2. Klik "Create New Template"
3. Edit template:

```
Subject: New Contact from {{from_name}} - Portfolio

From: {{from_name}} ({{reply_to}})
Service: {{service}}
Subject: {{subject}}

Message:
{{message}}

---
Sent from Portfolio Contact Form
Date: {{current_date}}
```

4. Variables yang dipakai:
   - `{{from_name}}` → Nama pengirim
   - `{{reply_to}}` → Email pengirim
   - `{{subject}}` → Subjek
   - `{{service}}` → Layanan yang dipilih
   - `{{message}}` → Pesan

5. Klik "Save"
6. **COPY Template ID** (contoh: template_xyz789)

### STEP 4: Get Public Key

1. Klik "Account" (menu kiri)
2. Pilih "General"
3. Lihat "Public Key"
4. **COPY Public Key** (contoh: abcdefghijk123456)

---

## 📝 Implementasi di Website

### Option A: Ganti JavaScript di script.js

Edit file `script.js`, cari fungsi `initContactForm()` dan ganti dengan:

```javascript
// Contact Form with EmailJS
function initContactForm() {
    const contactForm = document.getElementById('contactForm');
    const formMessage = document.getElementById('formMessage');
    
    if (!contactForm) return;
    
    // Initialize EmailJS
    emailjs.init('YOUR_PUBLIC_KEY'); // Ganti dengan Public Key Anda
    
    contactForm.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // Validate form
        if (!validateForm()) {
            return;
        }
        
        const submitBtn = this.querySelector('button[type="submit"]');
        const originalBtnText = submitBtn.innerHTML;
        
        // Show loading
        submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Mengirim...';
        submitBtn.disabled = true;
        
        // Send email with EmailJS
        emailjs.sendForm(
            'YOUR_SERVICE_ID',     // Ganti dengan Service ID
            'YOUR_TEMPLATE_ID',    // Ganti dengan Template ID
            this
        )
        .then(function(response) {
            console.log('SUCCESS!', response.status, response.text);
            showMessage('Pesan berhasil dikirim! Terima kasih.', 'success');
            contactForm.reset();
        }, function(error) {
            console.log('FAILED...', error);
            showMessage('Gagal mengirim pesan. Silakan coba lagi atau hubungi via WhatsApp.', 'error');
        })
        .finally(() => {
            submitBtn.innerHTML = originalBtnText;
            submitBtn.disabled = false;
        });
    });
    
    // Rest of validation code stays the same...
}
```

### Option B: Tambahkan Script EmailJS di index.html

1. Buka `index.html`
2. Sebelum tag `</body>`, tambahkan:

```html
<!-- EmailJS SDK -->
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
<script type="text/javascript">
    (function(){
        emailjs.init("YOUR_PUBLIC_KEY"); // Ganti dengan Public Key Anda
    })();
</script>

<!-- Load script.js setelah EmailJS -->
<script src="script.js"></script>
```

3. Update atribut name di form (index.html):

```html
<form id="contactForm">
    <div class="form-row">
        <div class="form-group">
            <input type="text" name="from_name" placeholder="Nama Lengkap" required>
            <i class="fas fa-user"></i>
        </div>
        <div class="form-group">
            <input type="email" name="reply_to" placeholder="Alamat Email" required>
            <i class="fas fa-envelope"></i>
        </div>
    </div>

    <div class="form-group">
        <input type="text" name="subject" placeholder="Subjek" required>
        <i class="fas fa-tag"></i>
    </div>

    <div class="form-group">
        <select name="service" required>
            <option value="" disabled selected>Pilih Layanan</option>
            <option value="web-development">Web Development</option>
            <option value="installation">Instalasi Laptop/PC</option>
            <option value="software-install">Instalasi Software</option>
            <option value="office-word">Office Word Service</option>
            <option value="computer-service">Service Komputer</option>
            <option value="consultation">Konsultasi Teknis</option>
        </select>
        <i class="fas fa-cogs"></i>
    </div>

    <div class="form-group">
        <textarea name="message" placeholder="Detail kebutuhan Anda..." rows="5" required></textarea>
        <i class="fas fa-comment"></i>
    </div>

    <button type="submit" class="btn btn-primary btn-block">
        <i class="fas fa-paper-plane"></i> Kirim Pesan
    </button>

    <div id="formMessage"></div>
</form>
```

---

## 🔧 Konfigurasi Lengkap

Buka `script.js` dan edit bagian ini:

```javascript
// Initialize EmailJS with your Public Key
emailjs.init('YOUR_PUBLIC_KEY');

// In the form submit handler
emailjs.sendForm(
    'YOUR_SERVICE_ID',      // Service ID dari EmailJS
    'YOUR_TEMPLATE_ID',     // Template ID dari EmailJS
    contactForm
)
```

**Ganti:**
- `YOUR_PUBLIC_KEY` → Public Key dari Step 4
- `YOUR_SERVICE_ID` → Service ID dari Step 2
- `YOUR_TEMPLATE_ID` → Template ID dari Step 3

---

## ✅ Testing

1. Upload file ke InfinityFree (htdocs/)
2. Buka website: http://yourdomain.com
3. Scroll ke form Contact
4. Isi form dan submit
5. Cek email Anda (yang diconnect di EmailJS)
6. Email masuk? → SUCCESS! 🎉

---

## 🎯 Keuntungan EmailJS

✅ Tidak perlu PHP backend
✅ Tidak perlu SMTP configuration
✅ Bypass port blocking InfinityFree
✅ Gratis 200 email/bulan
✅ Dashboard untuk monitoring
✅ Auto-reply support
✅ Multiple templates
✅ Attachment support (upgrade)

---

## 📊 Monitoring

1. Login ke Emailjs.com
2. Klik "Dashboard"
3. Lihat:
   - Emails sent today
   - Monthly usage
   - Success/fail rate
   - Email history

---

## 💡 Tips

1. **Template Variables:**
   - Gunakan `{{variable_name}}` di template
   - Match dengan `name` attribute di form
   - Example: `<input name="from_name">` → `{{from_name}}`

2. **Error Handling:**
   ```javascript
   .then(function(response) {
       // Success
   }, function(error) {
       // Failed - tampilkan error ke user
   })
   ```

3. **Validation:**
   - Tetap validate form di frontend
   - EmailJS akan validate email format
   - Check required fields

4. **Limit:**
   - Free: 200 email/bulan
   - Upgrade: $7/mo untuk 1000 email

---

## 🔄 Migration dari PHP ke EmailJS

Jika sudah pakai PHP version:

1. Backup `send-email.php` (rename jadi `send-email.php.bak`)
2. Update `script.js` dengan code EmailJS
3. Add EmailJS script di `index.html`
4. Update form attributes (name → from_name, email → reply_to)
5. Test
6. Done!

---

## ❓ Troubleshooting

### Error: "Service ID is invalid"
**Solusi:** Double check Service ID di dashboard EmailJS

### Error: "Template ID is invalid"
**Solusi:** Double check Template ID di dashboard EmailJS

### Error: "Public Key is invalid"
**Solusi:** Copy ulang Public Key dari Account → General

### Email tidak masuk
**Solusi:**
1. Cek folder Spam
2. Cek quota (200/bulan)
3. Lihat dashboard EmailJS (ada error log?)
4. Re-connect email service

### Form tidak submit
**Solusi:**
1. Buka browser console (F12)
2. Lihat error message
3. Pastikan EmailJS script loaded
4. Check internet connection

---

## 📞 Support

EmailJS Support:
- Docs: https://www.emailjs.com/docs/
- Support: support@emailjs.com

Portfolio Support:
- WhatsApp: 0895-4063-98540
- Email: renaldiyogapratama092@gmail.com

---

## 🎓 Alternative: Formspree

Jika EmailJS tidak cocok, coba Formspree:
- https://formspree.io
- 50 submission/bulan (gratis)
- Lebih simple
- Tidak perlu JavaScript

---

**EmailJS adalah solusi TERBAIK untuk InfinityFree!** 🚀

Setup sekali, dan form kontak Anda akan berfungsi sempurna tanpa masalah port blocking!
